name: Docker Build and Push

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Log in to Docker Hub
      uses: docker/login-action@v3
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_TOKEN }}

    - name: Build Docker image
      run: |
        docker build -t ChaimaBenOmrane/pythonlist:${{ github.sha }} .
        docker tag ChaimaBenOmrane/pythonlist:${{ github.sha }} ChaimaBenOmrane/pythonlist:latest

    - name: Push Docker image
      run: |
        docker push ChaimaBenOmrane/pythonlist:${{ github.sha }}
        docker push ChaimaBenOmrane/pythonlist:latest